# CLAUDE.md — 営業資料作成用ワークスペースのお作法

このリポジトリは **営業用のプレゼン資料（ピッチ）を、かんたん・きれい・速く作る** ためのものです。
Claude（私）は、このファイルのルールに従って資料作成を手伝います。
ユーザーは Markdown や git の知識がなくても大丈夫です。チャットで頼むだけで進みます。

---

## 📁 フォルダ構成と役割

```
営業資料作成用/
├── CLAUDE.md          ← このファイル（ワークフロー・お作法）
├── README.md          ← 人間向けクイックスタート
├── drafts/            ← 案件ごとの作業フォルダをここに切る（実作業はここ）
├── templates/
│   ├── standard-pitch.md     ← Marpベースのピッチ雛形（Addnessブランド：黒×青）
│   └── outline-template.md   ← まず構成案を書くテンプレ
├── assets/
│   ├── images/        ← 図・写真・スクショ
│   ├── logos/         ← ロゴ
│   └── icons/         ← アイコン
├── reference/         ← 過去資料への参照（symlink）。盗用ではなく「型」を学ぶ用
├── archive/           ← 完了・お蔵入り案件の置き場
└── themes/
    └── addness.css    ← ブランドテーマ（黒×青）
```

---

## 🔁 標準ワークフロー（三上さん流）

新しい案件の資料を作るときは、必ずこの順番で進めます。

1. **案件フォルダを切る** … `drafts/＜案件名＞/` を作る（例：`drafts/2026-06-製造業A社/`）
2. **まず構成案** … `templates/outline-template.md` をコピーして、骨子（課題→提案→効果→事例→料金→次の一歩）を埋める。**いきなりスライドを書かない。**
3. **構成をユーザーと確認** … 骨子がズレていないか一度すり合わせる
4. **スライド化** … `templates/standard-pitch.md` をコピーし、構成案の内容を流し込む
5. **過去資料を参照** … `reference/` の似た案件を「型」として参考にする（中身の丸写しはしない）
6. **出力** … PDF / PowerPoint に変換（下記コマンド）
7. **保存** … git に commit & push（消えないように必ず保存）
8. **完了したら archive へ** … 受注/失注が決まった案件は `drafts/` から `archive/` へ移す

---

## 🛠 スキル早見表（チャットでこう頼めばOK）

| やりたいこと | チャットでの頼み方の例 |
|---|---|
| 新規案件を始める | 「○○社向けの提案資料を作りたい。商品は△△、相手は□□業界」 |
| まず骨子を固める | 「先に構成案だけ作って」 |
| スライドにする | 「この構成でスライドにして」 |
| 文面の修正 | 「料金プランの表を書き換えて」「導入事例を製造業の例に」 |
| PDFにする | 「PDFにして」 |
| PowerPointにする | 「PowerPointにして」 |
| デザイン調整 | 「青をもう少し濃くして」「表紙のロゴを差し替えて」 |
| 過去資料を参考に | 「reference の○○案件を参考にして」 |
| 保存する | 「保存して」「GitHubに上げて」 |
| 完了案件を片付ける | 「この案件をアーカイブして」 |

---

## ✅ 資料作成のお作法（守ること）

- **構成案ファースト。** スライドより先に `outline-template.md` で骨子を固める。
- **1スライド1メッセージ。** 情報を詰め込みすぎない。
- **数字で語る。** 効果は Before/After の数字で示す（曖昧な「効率化」だけにしない）。
- **相手起点。** 自社の機能自慢より、相手の課題→解決の順で構成する。
- **ブランド統一。** 色・フォントは `themes/addness.css`（黒×青）に従う。勝手に色を散らさない。
- **必ずクロージング。** 最後のスライドは「次の一歩（無料相談・トライアル等）」で締める。
- **作業は drafts/ の中で。** templates/ の雛形は直接編集せず、コピーして使う。
- **作ったら push。** コンテナは一時的なので、保存しないと消える。

---

## 🎨 ブランド：Addness（黒 × 青）

- ベース：黒（`#0a0e16`） / アクセント：ブルー（`#1e6bff`）
- テーマファイル：`themes/addness.css`
- 表紙・章扉は黒背景、本文は白背景＋青の見出し。
- ロゴは `assets/logos/` に置いて表紙で参照する。

---

## 💻 変換コマンド（私が実行します。手動の場合の参考）

```bash
# 準備（最初の1回だけ）
npm install

# PDF
npx marp drafts/＜案件名＞/index.md --theme themes/addness.css --allow-local-files -o output/＜案件名＞.pdf

# PowerPoint
npx marp drafts/＜案件名＞/index.md --theme themes/addness.css --allow-local-files --pptx -o output/＜案件名＞.pptx
```

> `output/` と PDF/PPTX/HTML は `.gitignore` 済み（成果物はGitに含めない）。
> PDF / PowerPoint 変換には Chrome / Chromium が必要です（HTML出力は不要）。実行環境に無い場合は、私がHTMLで作成するか、Chromeのある環境での実行をご案内します。

---

## 🔗 reference/（過去資料）の使い方

`reference/` には、過去の優れた資料への **symlink（近道）** を置きます。実体はコピーしません。

```bash
# 例：過去資料フォルダへの近道を作る
ln -s "/path/to/ADDNESS-Sequoia-Materials-2026-05-02" reference/ADDNESS-Sequoia-Materials-2026-05-02
```

参照は「構成・言い回し・型」を学ぶため。中身の丸写しはしないこと。
